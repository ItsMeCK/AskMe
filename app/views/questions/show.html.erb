<p id="notice"><%= notice %></p>

<p>
  <strong>Question:</strong>
  <%= @question.question %>
</p>

<p>
  <strong>User:</strong>
  <%= @question.user_id %>
</p>

<h3>Answers</h3>

<%= nested_answers @answers %>

<%= render partial: 'answers/form', locals: { answer: @answer } %>

<%= link_to 'Edit', edit_question_path(@question) %> |
<%= link_to 'Back', questions_path %>
